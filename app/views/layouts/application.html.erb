<!DOCTYPE html>
<html>
  <head>
    <title><%= @title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
  <nav class="flex items-center justify-between flex-wrap p-6">
    <div class="flex items-center flex-shrink-0 mr-6">
      <span class="font-bold text-xl">
        <%= link_to("Рейтинг МАИИ", root_path) %>
      </span>
    </div>

    <div class="w-full block flex-grow sm:flex sm:items-center sm:w-auto">
      <div class="text-sm sm:flex-grow">
        <%= link_to("Команды", release_path, class: "block mt-4 sm:inline-block sm:mt-0 hover:font-bold mr-4") %>
        <%= link_to("Игроки", player_release_path, class: "block mt-4 sm:inline-block sm:mt-0 hover:font-bold mr-4") %>
        <%= link_to("Турниры", tournaments_path, class: "block mt-4 sm:inline-block sm:mt-0 hover:font-bold mr-4") %>
      </div>
    </div>
  </nav>
    <div class="container mx-auto max-w-max min-w-min p-2">
      <%= yield %>
    </div>
  </body>
</html>
