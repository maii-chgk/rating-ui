<%= link_to_previous_player_release_page(release_id: @release_id, current_top: @paging.from, current_bottom: @paging.to) %>

<% (1..5).each do |page| %>
  <%= link_to_player_release_page(release_id: @release_id, page: page, players_per_page: @paging.per_page) %>
<% end %>
...
<%= link_to_last_player_release_page(release_id: @release_id, players_in_release: @paging.items_count, players_per_page: @paging.per_page) %>

<%= link_to_next_player_release_page(release_id: @release_id, current_top: @paging.from, current_bottom: @paging.to) %>
