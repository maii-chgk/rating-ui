<table class="shadow-lg bg-white">
  <thead>
  <tr>
    <th class="bg-blue-100 border text-center px-2 py-2">Место</th>
    <th class="bg-blue-100 border text-center px-2">Команда</th>
    <th class="bg-blue-100 border text-center px-2">Состав</th>
    <th class="bg-blue-100 border text-center px-2">Взятые</th>
    <th class="bg-blue-100 border text-center px-2">Рейтинг</th>
    <th class="bg-blue-100 border text-center px-2">Дельта</th>
    <th class="bg-blue-100 border text-center px-2">Прогноз</th>
    <th class="bg-blue-100 border text-center px-2">D1</th>
    <th class="bg-blue-100 border text-center px-2">D2</th>
  </tr>
  </thead>

  <tbody>
  <% @tournament_results.each do |result| %>
    <tr class="border py-2 hover:bg-gray-100">
      <td class="border px-2 text-center"><%= round_place(result['place']) %></td>
      <td class="border px-2">
        <%= link_to(result['team_name'], team_path(team_id: result['team_id'])) %>
      </td>
      <td class="border px-2 py-2 text-left">
        <%= render partial: "shared/roster", collection: result['players'], as: :player %>
      </td>
      <td class="border px-2 text-center"><%= result['points'] %></td>
      <td class="border px-2 text-center"><%= result['rating'] %></td>
      <td class="border px-2 text-center"><%= result['rating_change'] %></td>
      <td class="border px-2 text-center"><%= result['forecast'] %></td>
      <td class="border px-2 text-center"><%= result['d1'] %></td>
      <td class="border px-2 text-center"><%= result['d2'] %></td>
    </tr>
  <% end %>
  </tbody>
</table>
